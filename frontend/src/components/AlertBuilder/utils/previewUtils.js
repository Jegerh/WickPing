import { getIndicatorByIdSync } from "../indicatorsRegistry";

// Generate condition text for previews
export const getConditionText = (block) => {
  // Implementation...
};

// Generate detailed preview
export const generateLogicPreview = (group, level = 0) => {
  // Implementation...
};

// Generate simple preview
export const generateSimpleLogicPreview = (group) => {
  // Implementation...
};